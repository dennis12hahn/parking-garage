<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.parkinggarage">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@drawable/ic_parking_lot_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_parking_lot_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".controller.tickets_and_receipts.TicketActivity"
            android:label="@string/title_activity_ticket"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".controller.users.attendant.actions.RetrieveActivity" />
        <activity android:name=".controller.users.attendant.actions.ParkActivity" />
        <activity android:name=".controller.users.attendant.AttendantActivity" />
        <activity android:name=".controller.users.manager.ManagerActivity" />
        <activity android:name=".controller.create_garage.CreateGarageActivity" />
        <activity android:name=".controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.SignInActivity" />
        <activity android:name=".controller.users.manager.actions.CreateAttendantActivity" />
    </application>

</manifest>